---
import BaseLayout from "../layouts/BaseLayout.astro";
const pageTitle = "Ich rufe Sie zurück!";
const SITE_URL = import.meta.env.SITE_URL || "https://www.musicstudio-ziebart.de";
const pageUrl = `${SITE_URL}/contact_4/`;
---

<BaseLayout
  pageTitle={pageTitle}
  description="Falls Sie Fragen haben, können Sie mir gerne eine kurze Mitteilung schicken. Ich werde Sie zurückrufen! 😀"
  keywords="ich rufe sie zurück, rückruf"
  href={pageUrl}
>
<br />
  <div class="container4">
    <form
      action="https://formsubmit.co/a7f052e0376799b4e2582093f684876f"
      method="POST"
    >
      <!-- Honeypot-Feld (versteckt) -->
      <input type="text" name="_honey" style="display:none">

      <!-- Verhindert Captcha-Anforderung -->
      <input type="hidden" name="_captcha" value="false">

      <!-- Leitet nach Absenden auf die Danke-Seite weiter -->
      <input type="hidden" name="_next" value="https://www.musicstudio-ziebart.de/danke-seite/">

      <div>
        <label for="name">Name:</label>
        <input
          type="text"
          autocomplete="name"
          id="name"
          name="name"
          placeholder="Namen eintragen"
          class="form-control form-control-lg"
          required
        />
      </div>

      <div>
        <label for="subject">Betreff:</label>
        <input
          name="subject"
          type="text"
          id="subject"
          maxlength="30"
          placeholder="Rückruf"
          class="form-control form-control-lg"
        />
      </div>

      <div>
        <label for="message">Nachricht:</label>
        <textarea
          id="message"
          name="message"
          cols="23"
          rows="10"
          placeholder="Bitte um Rückruf"></textarea>
      </div>

      <div>
        <label for="phone">Telefonnummer:</label>
        <input
          type="tel"
          id="phone"
          autocomplete="tel-national"
          name="phone"
          class="form-control form-control-lg"
          required
        />
      </div>

      <div>
        <label for="email">E-Mail:</label>
        <input
          type="email"
          id="email"
          autocomplete="email"
          name="email"
          class="form-control form-control-lg"
          required
        />
      </div>

      <br />
      <button type="submit" class="btn btn-lg btn-primary">Absenden</button>
    </form>
  </div>
</BaseLayout>
<p>
  <a href="../index.html" class="normal-links">Zurück</a>
</p>
