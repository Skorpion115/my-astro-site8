---
import BaseLayout from "../layouts/BaseLayout.astro";
import { getPageUrl } from "../utils/getPageUrl.js";
const pageTitle = "Ich rufe Sie zurück!";
const description = "Falls die Fragen haben können Sie mir gerne eine kurze Mitteilung schicken. Ich werde sie zurück rufen! 😀";
const keywords = "rueckruf, callback, telephon rueckruf";
const pageUrl = getPageUrl(Astro);
---

<BaseLayout
  pageTitle={pageTitle}
  description={description}
  keywords={keywords}
  href={pageUrl}
  ogTitle={pageTitle}
  ogDescription={description}
  ogUrl={pageUrl}
  ogType="website"
  twitterTitle={pageTitle}
  twitterDescription={description}
  twitterUrl={pageUrl}
>
  <div class="container4">
    <form
      action="https://formsubmit.co/a7f052e0376799b4e2582093f684876f"
      method="POST"
    >
      <!-- Honeypot field (versteckt für Nutzer, sichtbar für Bots) -->
      <p style="display:none">
        <label
          >Bitte nicht ausfüllen: <input type="text" id="bot-field" name="bot-field" /></label
        >
      </p>
      <input type="hidden" name="_captcha" value="false" />
      <input
        type="hidden"
        name="_next"
        value="https://www.musicstudio-ziebart.de/danke-seite/"
      >
      <div>
        <label for="name">Name:</label>
        <input
          type="text"
          autocomplete="name"
          id="name"
          name="name"
          placeholder="Namen eintragen"
          class="form-control form-control-lg"
          required
        >
      </div>
      <div>
        <label for="subject">Betreff:</label>
        <input
          name="subject"
          type="text"
          id="subject"
          maxlength="30"
          placeholder="Rückruf"
          class="form-control form-control-lg"
        >
      </div>
      <div>
        <label for="message">Nachricht:</label>
        <textarea
          id="message"
          name="message"
          cols="23"
          rows="10"
          class="form-control form-control-lg"
          placeholder="Bitte um Rückruf"></textarea>
      </div>
      <div>
        <label for="phone">Telefonnummer:</label>
        <input
          type="tel"
          id="phone"
          autocomplete="tel-national"
          name="phone"
          class="form-control form-control-lg"
          required
        >
      </div>
      <div>
        <label for="email">E-Mail:</label>
        <input
          type="email"
          id="email"
          autocomplete="email"
          name="email"
          class="form-control form-control-lg"
        >
      </div>
      <div>
        <label for="consent">
          <input type="checkbox" id="consent" name="consent" required />
          Ich stimme der Verarbeitung meiner Daten gemäß der
          <a href="/datenschutz/" target="_blank">Datenschutzerklärung</a>
          zu.
        </label>
      </div>
      <br />
      <button type="submit" class="btn btn-lg btn-primary">Absenden</button>
    </form>
  </div>
</BaseLayout>
