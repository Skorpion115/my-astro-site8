---
import BaseLayout from "../../layouts/BaseLayout.astro";
import { getPageUrl } from "../../utils/getPageUrl.js";

/* --------------------------------------------------
   Token aus URL lesen
-------------------------------------------------- */
const token = Astro.url.searchParams.get("token");

/* --------------------------------------------------
   Buildzeit verhindern
   Netlify Blobs nur bei Runtime abrufen
-------------------------------------------------- */
let valid = false;

if (typeof process.env.NETLIFY !== "undefined" && token) {
  const { getStore } = await import("@netlify/blobs");
  const store = getStore("download-tokens");

  const entry = await store.get(token);

  if (entry) {
    const text = new TextDecoder().decode(entry);
    const data = JSON.parse(text);

    valid =
      data.product === "yesterday" &&
      new Date(data.expires) > new Date();
  }
}

/* --------------------------------------------------
   SEO / Meta
-------------------------------------------------- */
const pageTitle = "Danke – Dein Download ist bereit";
const description =
  "Vielen Dank für deine Zahlung! Dein Noten- & Tabulaturblatt ‚Yesterday‘ ist jetzt zum Download verfügbar.";
const keywords = "Yesterday, Noten, Tabulatur, Download Noten, PDF";
const pageUrl = getPageUrl(Astro);
---

<BaseLayout
  pageTitle={pageTitle}
  description={description}
  keywords={keywords}
  href={pageUrl}
  ogTitle={pageTitle}
  ogDescription={description}
  ogUrl={pageUrl}
  ogType="website"
  twitterTitle={pageTitle}
  twitterDescription={description}
  twitterUrl={pageUrl}
>
  <h1>Danke für deine Zahlung!</h1>

  {valid ? (
    <>
      <h2>Dein Download ist bereit</h2>

      <p>
        <a href="/downloads/Yesterday.pdf" download class="cta-button">
          Yesterday – Noten &amp; Tabulatur
        </a>
      </p>

      <p>
        <a href="/downloads/Yesterday-Strings.pdf" download class="cta-button">
          Yesterday – Strings / Streicherstimmen
        </a>
      </p>

      <p>
        Viel Freude beim Spielen! 🎵
      </p>
    </>
  ) : (
    <>
      <h2>Download nicht verfügbar</h2>
      <p>
        ❌ Dieser Download-Link ist ungültig oder abgelaufen.
      </p>
    </>
  )}

  <p>
    Falls es Probleme mit dem Download gibt, schreib mir bitte eine kurze
    E-Mail:<br />
    <strong>postmaster@musicstudio-ziebart.de</strong>
  </p>
</BaseLayout>
