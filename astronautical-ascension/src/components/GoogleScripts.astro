<!-- Google Tag Manager laden -->
<script async>
  (function(w, d, s, l, i) {
    w[l] = w[l] || [];
    w[l].push({ "gtm.start": new Date().getTime(), event: "gtm.js" });
    var f = d.getElementsByTagName(s)[0],
        j = d.createElement(s),
        dl = l != "dataLayer" ? "&l=" + l : "";
    j.async = true;
    j.src = "https://www.googletagmanager.com/gtm.js?id=" + i + dl;
    f.parentNode.insertBefore(j, f);
  })(window, document, "script", "dataLayer", "GTM-PWK4LSL");

  // Warten auf Zustimmung des Nutzers
  document.addEventListener('DOMContentLoaded', function() {
    document.addEventListener('cmpEvent', function(event) {
      var consentData = event.detail;

      // Wenn der Nutzer zugestimmt hat, initialisiere die Tags
      if (consentData && consentData.cmpGDPR === 1) {
        gtag("config", "G-CXBFDX52C7", {
          "anonymize_ip": true,
          "currency": "EUR",
          "region": "EU",
          "linker": {
            "domains": ["musicstudio-ziebart.de", "www.musicstudio-ziebart.de"]
          }
        });

        gtag("config", "AW-1000620606", {
          "anonymize_ip": true,
          "currency": "EUR",
          "region": "EU",
          "linker": {
            "domains": ["musicstudio-ziebart.de", "www.musicstudio-ziebart.de"]
          }
        });
      }
    });
  });
</script>
